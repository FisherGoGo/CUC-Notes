# 信号的描述与分类

## 描述

- 按照**物理属性**分为**电信号**和**非电信号**
- 电信号的==基本形式==：随时间变化的电压或电流
- 描述信号常用的方法：**函数与波形**
	- 表示为一个或者多个独立变量的*函数*
	- 信号的图像表示*波形*
## 分类

### 确定性信号和随机信号

- 确定性信号：可用确定的函数或者波形表示
- 随机信号：无法用确定的函数式或者波形表示（不可预知）
### 连续（时间）信号和离散（时间）信号

- 连续信号：指**定义域连续**，函数值可连续也可不连续

![[1-1.png]]

- 离散信号：
	- 定义域：时间是离散的，只在某些离散瞬间有定义
	- 离散点间隔可以相等也可以不相等，通常取等间隔 $T$ ，离散信号可表示为 $f(kT)$ ，简写为 $f(k)$ ，这些间隔的离散信号常称为**序列**
	- 函数值可连续也可离散：幅值离散是*数字信号*，幅值连续是*抽样信号*
	- 通常取对应某序号 $m$ 的序列值称为第 $m$ 个样点的“样值”
### 周期信号与非周期信号

- 周期信号：在 $(-\infty,\infty)$ 区间，每隔一段时间 $T$ 按照相同规律重复变化的信号，周期 $T$ 为最小间隔
$$
f(t)=f(t+mT)\quad m=0,\pm1,\pm2,\ldots
$$
- 非周期信号：不具有重复性
- 若两个周期信号 $x(t),y(t)$ 的周期为 $T_{1}$ 与 $T_{2}$ ，若周期之比为有理数，那么 $x(t)+y(t)$ 的周期为他们的*最小公倍数*
### 能量（有限）信号与概率（有限）信号

> 归一化：信号在 z 单位电阻上消耗的能量或功率

- 瞬时功率：$p(t)=|f(t)|^{2}$ ，区间能量：$\int_{-a}^{a}|f(t)|^{2} \text{d} t$
$$
\begin{cases}连续信号能量:\quad E=\lim_{a \rightarrow\infty} \int_{-a}^{a}|f(t)|^{2} \text{d} x \\
连续信号功率:\quad P=  \lim_{a \rightarrow\infty} \dfrac{1}{2a} \int_{-a}^{a}|f(t)|^{2} \text{d} x\end{cases}
$$
$$
\begin{cases}离散信号能量:\quad E=\lim_{N \rightarrow\infty} \sum\limits_{k=-N}^N|f(k)|^{2} \\
连续信号功率:\quad P=  \lim_{N \rightarrow\infty} \dfrac{1}{2N+1} \sum\limits_{k=-N}^N|f(k)|^{2}\end{cases}
$$
- 能量信号：*能量有界*而功率为零，$0<E<\infty,P=0$
- 功率信号：能量无穷而*功率有界*，$0<P<\infty,E=\infty$
- 否则为非功率非能量信号
	- 时宽有限的有界信号都是能量信号
	- 直流、有界周期信号都是功率信号
	- 发散信号一般是非能量非功率信号：功率无穷大，如 $f(t)=e^{t}$ 
### 实信号与复信号

- 实信号：各时刻函数值为实数
- 复信号：存在某时刻函数值为复数
# 基本信号

## 连续信号

### 单位阶跃信号

$$
\varepsilon(t)= \begin{cases}0\quad t<0\\ 1\quad t>0\end{cases}
$$

```tikz
\begin{document}
\begin{tikzpicture}
    % --- 第一步：画坐标轴 ---
    % [->] 表示带箭头
    % node[right] {$t$} 表示在线的末端右边标个 t
    \draw[->] (-2,0) -- (4,0) node[right] {$t$}; 
    \draw[->] (0,-1) -- (0,2) node[above] {$\varepsilon(t)$};

    % --- 第二步：画 t<0 的部分 (值为0) ---
    % [very thick] 表示加粗，[red] 表示红色
    \draw[very thick, red] (-2,0) -- (0,0);

    % --- 第三步：画 t>0 的部分 (值为1) ---
    \draw[very thick, red] (0,1) -- (3.5,1);

    % --- 第四步：画中间的跳变 (虚线) ---
    \draw[dashed, red] (0,0) -- (0,1);

    % --- 第五步：标注数值 ---
    % 在 (0,0) 左下方标个 0
    \node[below left] at (0,0) {$0$}; 
    % 在 (0,1) 左边标个 1
    \node[left] at (0,1) {$1$};

\end{tikzpicture}
\end{document}
```
- 延时
$$
\varepsilon(t-t_{0})= \begin{cases}0\quad t<t_{0}\\ 1\quad t>t_{0}\end{cases}
$$
```tikz
\begin{document}
\begin{tikzpicture}
    % --- 第一步：画坐标轴 ---
    % [->] 表示带箭头
    % node[right] {$t$} 表示在线的末端右边标个 t
    \draw[->] (-2,0) -- (4,0) node[right] {$t$}; 
    \draw[->] (0,-1) -- (0,2) node[above] {$\varepsilon(t)$};

    % --- 第二步：画 t<0 的部分 (值为0) ---
    % [very thick] 表示加粗，[red] 表示红色
    \draw[very thick, red] (-2,0) -- (1,0);

    % --- 第三步：画 t>0 的部分 (值为1) ---
    \draw[very thick, red] (1,1) -- (3.5,1);

    % --- 第四步：画中间的跳变 (虚线) ---
    \draw[dashed, red] (1,0) -- (1,1);

    % --- 第五步：标注数值 ---
    % 在 (0,0) 左下方标个 0
    \node[below left] at (0,0) {$0$}; 
    % 在 (0,1) 左边标个 1
    \node[left] at (0,1) {$1$};
    \node[below left] at (1,0) {$t_0$};

\end{tikzpicture}
\end{document}
```

- 物理意义：表示信号单边性与其起作用的范围（时间）
### 符号信号
$$
sgn(t)= \begin{cases}1\quad t<0\\ -1\quad t>0\end{cases}
$$
```tikz
\begin{document}
\begin{tikzpicture}[>=latex] % 使用更美观的箭头
    % --- 1. 坐标轴和标题 ---
    % t轴
    \draw[->, thick] (-3,0) -- (3.5,0) node[right] {\Large $t$};
    % 纵轴
    \draw[->, thick] (0,-2) -- (0,2);
    % 标题 (放在纵轴上方)
    \node[above] at (0,2) {\Large $\mathbf{sgn}(t)$};

    % --- 2. 函数图像主体 (加粗) ---
    % t > 0 的部分 (从 (0,1) 向右的射线)
    \draw[very thick] (0,1) -- (3,1);
    
    % t < 0 的部分 (从 (0,-1) 向左的射线)
    \draw[very thick] (-3,-1) -- (0,-1);
    
    % t = 0 的部分 (原点实心点)
    \filldraw[black] (0,0) circle (2pt);

    % --- 3. 关键点标注 ---
    % 标注 y=1 (加个小刻度线更清晰)
    \draw[thick] (0,1) -- (-0.15,1) node[left] {\Large $1$};
    
    % 标注 y=-1
    \draw[thick] (0,-1) -- (-0.15,-1) node[left] {\Large $-1$};
    
    % 标注 原点 0 (放在右下角)
    \node[below right] at (0,0) {\Large $0$};

\end{tikzpicture}
\end{document}
```

### 单位冲激函数
$$
\delta(t)= \begin{cases}1\quad t=0\\ 0\quad t\ne 0\end{cases}
$$
```tikz
\begin{document}
\begin{tikzpicture}[>=latex, thick] % 全局设置箭头样式和线条粗细
    % --- 1. 绘制坐标轴 ---
    % t 轴 (横轴)
    \draw[->] (-3, 0) -- (3.5, 0) node[right] {\Large $t$};
    % 纵轴 (不需要标具体的 y，因为大部分是 0)
    \draw[->] (0, -0.5) -- (0, 2.5);
    
    % 标注原点
    \node[below left] at (0,0) {$0$};
    
    % 标注函数名 (放在纵轴顶端上方)
    \node[above] at (0, 2.5) {\Large $\delta(t)$};

    % --- 2. 绘制冲激信号 (核心部分) ---
    % 用一个非常粗 (very thick) 的蓝色箭头表示
    % 从原点 (0,0) 指向 (0, 1.8)
    \draw[->, very thick, blue] (0, 0) -- (0, 1.8);
    
    % --- 3. 标注强度/面积 ---
    % 在箭头顶端的右侧标上 "1"，表示单位强度
    \node[right, blue] at (0, 1.8) {\Large $1$};

\end{tikzpicture}
\end{document}
```

- 物理意义：作用时间短，取值极大而效果有限的物理现象，如力的冲量
- 与阶跃信号的关系：
$$
\begin{cases}\varepsilon(t) = \int_{-\infty}^{t} \delta(x) \, dx \\ \delta(t) = \frac{d}{dt} \varepsilon(t)\end{cases}
$$
- 性质：
	- 当 $f(t)$ 为任意有界函数：
$$
\begin{cases}f(t)\delta(t)=f(0)\delta(t) \\
f(t)\delta(t-t_{0})=f(t_{0})\delta(t-t_{0})\end{cases}
$$
	- 抽样性：
		- $\int_{-\infty}^{\infty}f(t)\cdot\delta(t-t_{0})\, dt=f(t_{0})$
		- $\int_{t_{1}}^{t_{2}}f(t)\cdot\delta(t-t_{0})\, dt=\begin{cases}f(t_{0}) \quad t_{1}<t_{0}<t_{2}\\ 0 \quad otherwise\end{cases}$
	- 尺度变换：$\delta(at-t_{0})= \frac{1}{|a|}\delta\left( t- \frac{t_{0}}{a} \right)$
	- 积分：$\int_{-\infty}^{t} f(x)\cdot\delta(x-x_{0})\, dx=f(x_{0})\cdot\varepsilon(t-x_{0})$
### 冲激偶信号
$$
\delta'(x)= \frac{d}{dt}\delta(t)
$$
- 性质：
	- $f(t)\cdot \delta'(t-t_{0})=f'(t_{0})\cdot\delta(t-t_{0})-f(t_{0})\cdot\delta'(t-t_{0})$
	- $\int_{-\infty}^{\infty}f(t)\cdot\delta'(t-t_{0})\, dt=-f'(t_{0})$
	- $\delta'(at)= \frac{1}{a|a|}\delta'(t)$
## 离散信号
### 单位序列
$$
\delta(k)= \begin{cases}0\quad k\ne0\\ 1\quad k=0\end{cases}
$$
### 单位阶跃序列
$$
\varepsilon(k)= \begin{cases}0\quad k<0\\ 1\quad k\ge0\end{cases}
$$
```tikz
\begin{document}
\begin{tikzpicture}[scale=0.8]
    % --- 绘制坐标轴 ---
    \draw[->] (-4.5,0) -- (6.5,0) node[right] {$n$};
    \draw[->] (0,-0.5) -- (0,1.5) node[above] {$u[n]$};

    % --- 绘制刻度 ---
    \foreach \x in {-4,-3,...,6}
        \draw (\x, 0.1) -- (\x, -0.1) node[below, font=\footnotesize] {$\x$};
    \draw (0.1, 1) -- (-0.1, 1) node[left, font=\footnotesize] {$1$};

    % --- 绘制 n < 0 的点 (y=0) ---
    \foreach \x in {-4,-3,-2,-1} {
        \draw[fill=blue!70!black, color=blue!70!black] (\x,0) circle (2pt);
    }

    % --- 绘制 n >= 0 的点 (y=1) ---
    \foreach \x in {0,1,...,6} {
        \draw[color=blue!70!black, thick] (\x,0) -- (\x,1); % 火柴杆
        \draw[fill=blue!70!black, color=blue!70!black] (\x,1) circle (2pt); % 顶点
    }
\end{tikzpicture}
\end{document}
```

- $\delta(k)=\varepsilon(k)-\varepsilon(k-1)$
- $\varepsilon(k)=\sum\limits_{j=0}^{\infty}\delta(k-j)=\sum\limits_{j=-\infty}^{k}\delta(j)$

### 正弦序列
$$
f(k)=\cos(\omega_{0}k)=\cos(\omega_{0}k+2m\pi)=\cos\left( \omega_{0}\left( k+m \frac{2\pi}{\omega_{0}} \right) \right)
$$
- 当仅当 $m \frac{2\pi}{\omega_{0}}$ 为整数时有周期 $m \frac{2\pi}{\omega_{0}}$ ，既 $\frac{2\pi}{\omega}$ 为有理数
- 周期为 $\frac{\omega_{0}}{2\pi}=\frac{m}{N}$ ，其中 $T=N$ 
- 若有多个正弦序列相加，周期为 $\mathbf{lcm}(T_{1},T_{2},\ldots,T_{k})$ 
### 复指数序列
$$
f(k)=e^{(\alpha+j\omega_{0})k}=e^{\alpha k}\cdot e^{j\omega_{0} k}
$$
- $\alpha>0$ , $k$ 越大，$f(k)$ 幅度越大，反之越小

# 线性时不变系统（LTI）

```tikz
\begin{document}
\begin{tikzpicture}[>=latex, thick]
    % --- 1. 定义三个关键节点 (Input, Block, Output) ---
    
    % 输入端子：定义为一个圆形节点，命名为 input
    % inner sep=1.5pt 控制圆圈的大小
    \node[circle, draw, inner sep=1.5pt] (input) at (0,0) {};
    
    % 中间的系统框：定义为矩形节点，命名为 block
    % minimum width/height 控制方框尺寸
    \node[draw, very thick, rectangle, minimum width=2.5cm, minimum height=1.2cm] 
        (block) at (3.5,0) {\Large $LTI$};
        
    % 输出端子：定义为圆形节点，命名为 output
    \node[circle, draw, inner sep=1.5pt] (output) at (7,0) {};


    % --- 2. 添加文字标签 ---
    
    % 在 input 节点的上方
    \node[above=0.2cm] at (input) {\Large $e(g)$};
    
    % 在 output 节点的上方
    \node[above=0.2cm] at (output) {\Large $y(g)$};


    % --- 3. 智能连线 ---
    
    % 这里的魔法是：直接用节点名称连线
    % TikZ 会自动计算，从圆圈的边缘连到方框的边缘，不会插进图形内部
    \draw[->] (input) -- (block);
    \draw[->] (block) -- (output);

\end{tikzpicture}
\end{document}
```

$$
y(g)=T[e(g)]
$$
## 线性性质

- 均匀性：$T[k\cdot e(g)]=k\cdot T[e(g)]=k\cdot y(g)$
- 叠加性：$T[e_{1}(g)+e_{2}(g)]=y_{1}(g)+y_{2}(g)$

```tikz
\begin{document}
\begin{tikzpicture}[>=latex, thick]
    % --- 1. 中间的 LTI 系统框 ---
    % 定义为矩形，加粗边框 (very thick)
    \node[draw, rectangle, very thick, minimum width=2.5cm, minimum height=1.2cm] 
        (block) at (0,0) {\Large $LTI$};

    % --- 2. 输入部分 (左侧) ---
    % 定义输入端的小圆圈 (input terminal)
    % 位置在 block 左边 3cm 处
    \node[circle, draw, inner sep=1.5pt] (in_term) at (-3, 0) {};
    
    % 画箭头：从端子指向 block
    \draw[->] (in_term) -- (block);
    
    % 放输入公式
    % anchor=east 表示公式的“右侧”锚点对齐在端子的左边
    % left=0.2cm 表示留一点空隙
    \node[left=0.2cm] at (in_term) {\Large $K_1 e_1(g) + K_2 e_2(g)$};


    % --- 3. 输出部分 (右侧) ---
    % 定义输出端的小圆圈
    % 位置在 block 右边 3cm 处
    \node[circle, draw, inner sep=1.5pt] (out_term) at (3, 0) {};
    
    % 画箭头：从 block 指向端子
    \draw[->] (block) -- (out_term);
    
    % 放输出公式
    % anchor=west 表示公式的“左侧”锚点对齐在端子的右边
    \node[right=0.2cm] at (out_term) {\Large $K_1 y_1(g) + K_2 y_2(g)$};

\end{tikzpicture}
\end{document}
```
## 线性动态系统

```tikz
\begin{document}
\begin{tikzpicture}[>=latex, thick]
    % --- 1. LTI 系统框 ---
    % 定义为矩形，加粗边框
    \node[draw, rectangle, very thick, minimum width=2.5cm, minimum height=1.2cm] 
        (block) at (0,0) {\Large $LTI$};

    % --- 2. 输入部分 (左侧) ---
    % 定义输入端的小圆圈
    \node[circle, draw, inner sep=1.5pt] (in_term) at (-3, 0) {};
    
    % 画箭头：从端子指向 block
    \draw[->] (in_term) -- (block);
    
    % 放输入公式 {e(g)}
    \node[left=0.2cm] at (in_term) {\Large $\{e(g)\}$};

    % --- 3. 初始条件标注 ---
    % 在 LTI 框的上方标注 {x(0)}
    \node[above=0.8cm] at (block) {\Large $\{x(0)\}$};

    % --- 4. 输出部分 (右侧) ---
    % 定义输出端的小圆圈
    \node[circle, draw, inner sep=1.5pt] (out_term) at (3, 0) {};
    
    % 画箭头：从 block 指向端子
    \draw[->] (block) -- (out_term);
    
    % 放输出公式 y(g)
    \node[above=0.2cm] at (out_term) {\Large $y(g)$};

\end{tikzpicture}
\end{document}
```

- $\{e(g)\}$ 为外加激励，$\{x(0)\}$ 为初始状态
## 如何判断线性时不变
### 形如 $a_{n}y^{(n)}+\ldots+y=b_{m}f^{(m)}+\ldots+f$

- 线性：
	- 所有 $y$ 与 $f$ 及其导数项或者平移项次幂必须是 $1$ ，若出现 $(f^{(x)})^{k}\,\,\, k\ne 1$ 则非线性
	- 不能出现 $y$ 与 $f$ 相乘项
	- 不能被非线性函数包裹如 $\sin(y(t))$
	- 不能有非零常数项（零输入零输出）
- 时变：
	- 系数 $a_{i},b_{j}$ 必须是常数
	- 信号内部不能有时间缩放或者反转（如果方程两边都有同样的缩放，它们**可能**会相互抵消，从而变成时不变）
	- 方程中不能有单独的 $t$ 或 $k$ 项
### 形如 $y(t)=F(t)$

- 线性：
	- 先判断分解性，使初始状态为零得到 $y_{zi}$ ，再令输入为零得到 $y_{zs}$ ，必须得 $y_{zi}+y_{zs}=y$
	- 然后分别判断零输入和零状态是不是都是线性的
- 时变：
	- 设输入为 $f(t-t_{0})$ 得到 $y_{1}(t)$ ，看是否 $y_{1}(t)=y(t-t_{0})$ 
### 性质

- 分解特性：$y(g)=T[\{x(0)\},\{e(g)\}]=y_{zi}(g)+y_{zs}(g)$
	- $y_{zi}(g)=T[\{x(0)\},\{0\}]$ 零输入 zero input
	- $y_{zs}(g)=T[\{0\},\{e(g)\}]$ 零状态 zero status
- 时不变性：$T[\{0\},\{e(t-t_{d})\}]=y_{zs}(t-t_{d})$
- 微分与积分性：
	- $T[\{0\},\{e^{(-n)}(t)\}]=y_{zs}^{(-n)}(t)$
	- $T[\{0\},\{e^{(n)}(t)\}]=y_{zs}^{(n)}(t)$
- 因果性：$e(t)=0\,(t<t_{0}) \Rightarrow y_{zs}(t)=0\, (t<t_{0})$
- 稳定性：$|e(t)|<\infty$ ，$|y(t)|<\infty$
