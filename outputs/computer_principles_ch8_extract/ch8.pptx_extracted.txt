# ch8.pptx
共 17 张幻灯片


## 幻灯片 1
### 第8章 串行接口
串行通信的基本概念
串行数据传输方式
串行通信的类型
串行接口和串行接口标准

## 幻灯片 2
### 串行通信的基本概念
通信的基本方式可分为：
并行通信：把一个字符的各数位用几根数据线同时进行传输。
串行通信：数据逐位顺序传送。
优点：用于通信的线路少，在远距离通信时可以极大地降低成本。
缺点：相对并行通信方式，串行通信速度较慢
PC机上一般都有两个串行异步通信接口COM1和COM2，键盘、鼠标器与主机之间也常采用串行数据传送方式。

## 幻灯片 3
### 串行数据传输方式
目前，在微机通信系统中，单工方式很少采用，多数是采用半双工或全双工方式。

## 幻灯片 4
### 串行通信的类型
串行通信时，数据、控制和状态信息都使用同一根信号线传送。为使通信能顺利进行，收发双方必须遵守共同的通信协议(通信规程)，才能解决传送速率、信息格式、同步方式、数据校验等问题。
根据同步方式的不同，将串行通信分为两类:
异步通信(asynchronous communication)
同步通信(synchronous communication)

## 幻灯片 5
### 串行通信的类型
异步通信
异步通信以字符为单位(character –oriented)进行传输，采用起止式异步通信协议。

## 幻灯片 6
### 串行通信的类型
每个字符开始传送的标志，起始位采用逻辑0电平
表示该字符传送结束。停止位采用逻辑1电平，可选择1、1.5或2位
数据位紧跟着起始位传送。由5～8个二进制位组成，低位先传送
用于校验是否传送正确；可选择奇检验、偶校验或不传送校验位
传送字符之间的逻辑1电平，表示没有进行传送

## 幻灯片 7
### 串行通信的类型
异步通信
异步通信的工作原理
传送开始后，接收设备不断检测传输线是否有起始位的到来，当接收到一系列的“1”(空闲位或停止位)之后，检测到第一个“0”，说明起始位出现，就开始接收所规定的数据位、奇偶校验位及停止位。
经过接收器处理，将停止位去掉，把数据位拼装成为一个字节数据，经校验无误，则接收完毕。
当一个字符接收完毕后，接收设备又继续监视传输线“0”电平的到来(下一个字符的开始)，直到全部数据接收完毕。

## 幻灯片 8
### 串行通信的类型
异步通信
异步通信的特点
优点：由于接收方通过每个字符的起始位达到字符传送的同步时钟作用，所以收/发双方设备较简单，实现起来方便，对各字符间的间隔长度没有限制。
缺点：每个数据要加上起始位、停止位等信息，至少要有20％的冗余时间，这样就降低了数据的传送速率。

## 幻灯片 9
### 串行通信的类型
同步通信
面向字符型的数据格式
从同步通信数据格式中可以看出，传送的数据信息越长，数据传输效率越高，故同步通信运用于要求快速、连续传输大量数据的场合。

## 幻灯片 10
### 串行通信的类型
波特率
串行通信的传输速率也称波特率(Baud Rate)，即每秒传输的二进制位数。标准波特率系列为110、300、600、1200、1800、2400、4800、9600和19200。现在，数据传输速率可以达到115200bps或更高。
例如，波特率为1200，对于采用1个停止位、不用校验的8位数据传送来说，一个字符共有10位，则每秒能传送120(=1200/10)个字符。

## 幻灯片 11
### 串行通信的类型
异步通信与同步通信的比较
在异步传输过程中，设每个字符对应1个起始位、7个数据位、1个奇/偶校验位和1个停止位，如果波特率为1200，则每秒钟能传输的最大字符数为:1200／10＝120个。
再来看一个同步传输的例子。假如也用1200的波特率工作，用2个同步字符和2个CRC码，设每秒钟能传输的字符数为n，则7×(n+2+2)=1200，n＝167个。
在同样的传输速率下，同步传输实际字符传输速率要比异步传输时高。

## 幻灯片 12
### 串行接口和串行接口标准
串行接口概述
串行通信实际上把数据一位一位地发送和接收，而计算机处理数据是并行的，它要传输的数据也是并行的，因此这就需要一个部件把并行数据与串行数据进行转换。
发送数据端：CPU把要传输的数据写入并入串出移位寄存器中，然后移位寄存器在同步时钟的作用下，把数据逐位移出，发送给接收端。
接收数据端：在同步时钟的作用下，将发送端送来的数据逐位移入串入并出移位寄存器，然后CPU把串入并出移位寄存器的数据读入CPU中。

## 幻灯片 13
### 串行接口和串行接口标准
串行接口标准RS-232C
RS-232C是由美国电子工业协会(Electronics Industries Association，EIA)于1962年公布，并于1969年修订的一种国际通用的串行接口标准。
现已成为数据终端设备DTE（例如计算机）与数据通信设备DCE（例如调制解调器）的标准接口。
可实现远距离通信，也可近距离连接两台微机。
232C接口标准使用一个25针连接器。绝大多数设备只使用其中9个信号，所以就有了9针连接器。
PC机上的COM1、COM2接口就是RS-232C接口。

## 幻灯片 14
### 串行接口和串行接口标准
RS-232C采用EIA电平（0高电平5~15V，1低电平-5~-15V，负逻辑）

## 幻灯片 15
### 串行接口和串行接口标准
使用MODEM的RS-232C接口

## 幻灯片 16
### 串行接口和串行接口标准
RS-232C的三线连接方法
由于通信双方并未进行联络应答，所以采用三线连接方式时，应注意传输的可靠性。

## 幻灯片 17
### 小结
本节课讲授了：
串行通信的基本概念：串行通信和并行通信。
串行数据传输方式：单工、半双工和全双工。
串行通信的类型：异步通信和同步通信，重点讲授了起止式异步通信协议，波特率的概念。
串行接口和串行接口标准：RS-232C串行接口标准。