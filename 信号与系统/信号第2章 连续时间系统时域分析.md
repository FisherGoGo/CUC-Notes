# 系统微分方程经典解

- 对于一个系统方程：
$$
a_{n}y^{(n)}(t)+a_{n-1}y^{(n-1)}(t)+\ldots+a_{0}y(t)=b_{m}e^{(m)}(t)+\ldots+b_{0}e(t)
$$
- 我们有：$y(t)=y_{h}(t)+y_{p}(t)$
	- $y_{h}(t)$ 称为*自由响应*或*齐次解*
	- $y_{p}(t)$ 称为*强迫响应*或*特解*
## 齐次解

- 我们有齐次方程：
$$
a_{n}y^{(n)}(t)+a_{n-1}y^{(n-1)}(t)+\ldots+a_{0}y(t)=0
$$
- 得到特征方程：
$$
\lambda^{n}+a_{n-1}\lambda^{n-1}+\ldots+a_{1}\lambda+a_{0}
=0$$
- 我们可以得到 $n$ 个解 $\lambda_{i}\,(i=1,2,\ldots,n)$
- 从而得到 $y_{h}$ ，详情查[[附录]]
## 特解：

- 由激励 $e(t)$ 形式决定，详情查[[附录]]

## 系统在 $t=0_{+}$ 与 $t=0_{-}$ 状态

- 在 $0_{-} \rightarrow0_{+}$ 时，系统 $y(t)$ 可能发生跳变，使得 $y(0_{-})\ne y(0_{+})$
- 我们称 $y(0_{-})$ 为初始状态，$y(0_{+})$ 为初始条件
- 有 $初始条件=初始状态+跳变量$ ，既 $y(0_{+})=y(0_{-})+y_{zs}(0_{+})=y_{zi}(0)+y_{zs}(0)$
- 发生跳变的条件：微分方程右侧含 $\delta(t)$ 及其各阶导数
> 跳变量的确定方法：$\delta(t)$ 匹配法，平衡后 $y^{(i)}(t)$ 含的 $\varepsilon(t)$ 系数即为跳变量
# 零输入与零状态响应

## 零输入响应

- 无外加响应，仅有初始状态，对应齐次方程
- 初始条件=初始状态
## 零状态响应

- 初始状态为 $0$ ，仅有输入信号，对应非齐次方程
- 初始条件=跳变量
# 冲激响应与阶跃响应
## 冲激响应

```tikz
\begin{document}
\begin{tikzpicture}[>=latex, thick]
    % --- 1. LTI 系统框 ---
    % 定义为矩形，加粗边框
    \node[draw, rectangle, very thick, minimum width=2.5cm, minimum height=1.2cm] 
        (block) at (0,0) {\Large $LTI$};

    % --- 2. 输入部分 (左侧) ---
    % 定义输入端的小圆圈
    \node[circle, draw, inner sep=1.5pt] (in_term) at (-3.5, 0) {};
    
    % 画箭头：从端子指向 block
    \draw[->] (in_term) -- (block);
    
    % 输入公式：e(t) = delta(t)
    %放在端子上方 (above)
    \node[above=0.1cm] at (in_term) {\Large $e(t) = \delta(t)$};

    % --- 3. 初始状态条件 ---
    % 在 LTI 框正上方标注 {x(0)} = 0
    \node[above=0.7cm] at (block) {\Large $\{x(0)\} = 0$};

    % --- 4. 输出部分 (右侧) ---
    % 定义输出端的小圆圈
    \node[circle, draw, inner sep=1.5pt] (out_term) at (3.5, 0) {};
    
    % 画箭头：从 block 指向端子
    \draw[->] (block) -- (out_term);
    
    % 输出公式：y_zs(t) = h(t)
    % 注意：h(t) 是红色的，我们用 {\color{red} ...} 来变色
    \node[above=0.1cm] at (out_term) {\Large $y_{zs}(t) = {\color{red} h(t)}$};

\end{tikzpicture}
\end{document}
```
$$
h(t)=T[\{0\},\{\delta(t)\}]
$$
- 对应系统 $\sum\limits_{i=0}^{n}a_{i}h^{(i)}(t)=\sum\limits_{j=0}^{m}b_{j}\delta^{(j)}(t)$
- 由于 $\delta(t)$ 在 $t>0$ 时为 $0$ ，所以 $h(t)$ 的特解为 $0$ 

>  对于求 $h(t)$ ，我们可以使用间接法，我们令 $h_{1}(t)=\delta(t)$ ，求 $h_1(t)$ 是简单的，然后我们根据 $h(t)$ 与 $h_{1}(t)$ 的线性关系可以快速求出 $h(t)$

- 冲激方程一般形式为:
	- 当 $n>m$ ：$h(t)=\sum\limits_{i}C_{i}e^{\lambda_{i}t}\varepsilon(t)$
	- 当 $n=m$ ：$h(t)=\sum\limits_{i}C_{i}e^{\lambda_{i}t}\varepsilon(t)+C_0\delta(t)$
	- 当 $n<m$ ：$h(t)=\sum\limits_{i}C_{i}e^{\lambda_{i}t}\varepsilon(t)+C_{0}\delta(t)+C_{0}'\delta'(t)+\ldots$
## 阶跃响应
```tikz
\begin{document}
\begin{tikzpicture}[>=latex, thick]
    % --- 1. LTI 系统框 ---
    % 定义为矩形，加粗边框
    \node[draw, rectangle, very thick, minimum width=2.5cm, minimum height=1.2cm] 
        (block) at (0,0) {\Large $LTI$};

    % --- 2. 输入部分 (左侧) ---
    % 定义输入端的小圆圈
    \node[circle, draw, inner sep=1.5pt] (in_term) at (-3.5, 0) {};
    
    % 画箭头：从端子指向 block
    \draw[->] (in_term) -- (block);
    
    % 输入公式：e(t) = delta(t)
    %放在端子上方 (above)
    \node[above=0.1cm] at (in_term) {\Large $e(t) = \varepsilon(t)$};

    % --- 3. 初始状态条件 ---
    % 在 LTI 框正上方标注 {x(0)} = 0
    \node[above=0.7cm] at (block) {\Large $\{x(0)\} = 0$};

    % --- 4. 输出部分 (右侧) ---
    % 定义输出端的小圆圈
    \node[circle, draw, inner sep=1.5pt] (out_term) at (3.5, 0) {};
    
    % 画箭头：从 block 指向端子
    \draw[->] (block) -- (out_term);
    
    % 输出公式：y_zs(t) = h(t)
    % 注意：h(t) 是红色的，我们用 {\color{red} ...} 来变色
    \node[above=0.1cm] at (out_term) {\Large $y_{zs}(t) = {\color{red} g(t)}$};

\end{tikzpicture}
\end{document}
```
$$
g(t)=[\{0\},\{\varepsilon(t)\}]
$$
- 与 $h(t)$ 的关系：
	- $\delta(t)= \frac{d}{dt}\varepsilon(t) \Rightarrow h(t)= \frac{d}{dt}g(t)$
	- $\varepsilon(t)=\int_{-\infty}\delta(x)\,dx \Rightarrow g(t)=\int_{-\infty}^{t}h(x)\, dx$

# 卷积积分

- 定义：设 $f_{1}(t)$ 与 $f_{2}(t)$ 是在 $(-\infty,\infty)$ 的连续函数，有
$$
f(t)=\int_{-\infty}^{\infty} f_{1}(x)\cdot f_{2}(t-x)\, dx=f_{1}(t)*f_{2}(t)
$$
- 积分限根据 $f_{1}(t)$ 与 $f_{2}(t)$ 自身定义域受限
- 若 $f_{1}(t)\,t\in(t_{1},t_{3})$ ，$f_{2}(t)\,t\in(t_{2},t_{4})$ ，那么 $f_{1}(t)*f_{2}(t)\, t\in(t_{1}+t_{2},t_{3}+t_{4})$
- 图示法求卷积积分：
	- 变量代换，我们令 $t=x$ 
	- 反折 $f_{2}(x) \rightarrow f_{2}(-x)$
	- 平移 $f_{2}(-x) \rightarrow f_{2}(t-x)$ ，当 $t$ 从 $-\infty$ 增大时 $f_{2}(t-x)$ 从左向右移动
	- 将 $f_{1}(x)$ 与 $f_{2}(t-x)$ 相乘后积分（既围成的面积）
## 试证 $f(t)=\delta(t)*f(t)$

```tikz
\begin{document}
\begin{tikzpicture}[>=stealth]
    % 1. 定义绘图参数（方便调整大小）
    \def\w{2}   % 宽度 Delta 代表的长度
    \def\h{2.5} % 高度 1/Delta 代表的长度

    % 2. 绘制坐标轴
    % X轴
    \draw[->, thick] (-0.5, 0) -- (\w+1.5, 0) node[right] {$t$};
    % Y轴
    \draw[->, thick] (0, -0.5) -- (0, \h+1);

    % 3. 绘制矩形脉冲 g_Delta(t)
    % 线条加粗 (ultra thick) 以匹配图片风格
    \draw[ultra thick] (0,0) -- (0,\h) -- (\w,\h) -- (\w,0);

    % 4. 添加标签
    % 原点 0 (加粗)
    \node[below left] at (0,0) {\textbf{0}};
    
    % Y轴刻度标签 1/Delta
    \node[left] at (0, \h) {$\frac{1}{\Delta}$};
    
    % X轴刻度标签 Delta
    \node[below] at (\w, 0) {$\Delta$};
    
    % 函数名称标签
    \node[right] at (0.2, \h+0.5) {\Large $g_\Delta(t)$};

\end{tikzpicture}
\end{document}
```

- 我们令 $\hat{f}(t)=\sum\limits_{n=-\infty}^{\infty}f(n\Delta)g_{\Delta}(t-n\Delta)\Delta$
- 当 $\Delta \rightarrow0$ 时，$\hat{f}(t) \rightarrow f(t)$
- 那么我们令 $\Delta \rightarrow d\tau$ ，$n\Delta \rightarrow\tau$ ，$g_{\Delta}(t)\rightarrow \delta(t)$ ，$\sum\limits_{n=-\infty}^{\infty} \rightarrow\int_{-\infty}^{\infty}$ 
- 那么就有 $\hat{f}(t)=\int_{-\infty}^{\infty}f(\tau)\cdot\delta(t-\tau)\,d\tau=f(t)=f(t)*\delta(t)$
### 利用卷积积分求解零状态响应

$$
e(t)=\lim_{\Delta\rightarrow 0} \sum\limits_{n=-\infty}^{\infty} e(n\Delta)\cdot \delta(t-n\Delta)\Delta \Rightarrow y_{zs}(t)=\lim_{\Delta\rightarrow 0} \sum\limits_{n=-\infty}^{\infty} e(n\Delta)\cdot h(t-n\Delta)\Delta
$$
- 得到 $y_{zs}(t)=e(t)*h(t)$ 
- 物理意义：将 $e(t)$ 分解成为无穷多个连续出现的冲激信号之和，借助系统的冲激响应、线性、时不变性质求解系统对任意信号激励下的零状态响应
## 卷积积分的性质

- 代数运算：
	- $f_{1}(t)*[f_{2}(t)+f_{3}(t)]=f_{1}(t)*f_{2}(t)+f_{1}(t)*f_{3}(t)$
	- $[f_{1}(t)*f_{2}(t)]*f_{3}(t)=f_{1}(t)*[f_{2}(t)*f_{3}(t)]$
- 与冲激函数的卷积：
	- $f(t-t_{1})*\delta(t-t_{2})=f(t-t_{1}-t_{2})$
- 微分与积分：
	- $[f_{1}(t)*f_{2}(t)]^{(k)}=f_{1}^{(i)}(t)*f_{2}^{(j)}(t)\quad i+j=k$
	- $\varepsilon(t)*\varepsilon(t)=t\varepsilon(t)$

