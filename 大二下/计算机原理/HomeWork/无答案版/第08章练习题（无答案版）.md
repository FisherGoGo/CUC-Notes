# 第八章练习题（无答案版）

## 1. 异步串行通信说法判断

**题目：** 下列有关异步串行通信的叙述中，正确的是（ ）。

**选项：**

- A. 在全双工方式下，收发双方只需要一根线相连
- B. 奇偶校验位的作用是检错与纠错
- C. 远程终端一定要通过 MODEM 才能与主机相连接
- D. 发送方与接收方无须同步

## 2. 异步通信传输信息的特点

**题目：** 异步通信传输信息时，其特点是（ ）。

**选项：**

- A. 字符发送速率由波特率确定
- B. 每个字符的发送是独立的
- C. 通信双方不必同步
- D. 字符之间的传输时间长度相同

## 3. 串并转换部件

**题目：** 串行接口中，并行数据和串行数据的转换是利用（ ）实现的。

**选项：**

- A. A/D 转换器
- B. 锁存器
- C. 移位寄存器
- D. 数据寄存器

## 4. 波特率含义

**题目：** 在串行通信中，使用波特率来表示数据的传输速率，它是指（ ）。

**选项：**

- A. 每秒传送的字符数
- B. 每秒传送的位数
- C. 每秒传送的字节数
- D. 每分钟传送的字符数

## 5. 串行接口的主要功能

**题目：** 对于串行接口，其主要功能为（ ）。

**选项：**

- A. 输入时将并行数据转换为串行数据，输出时将串行数据转换为并行数据
- B. 仅串行数据到并行数据的转换
- C. 仅并行数据到串行数据的转换
- D. 输入时将串行数据转换为并行数据，输出时将并行数据转换为串行数据

## 6. 异步通信区别于同步通信的特点

**题目：** 异步通信区别于同步通信的主要特点是（ ）。

**选项：**

- A. 每个字符的发送是独立的
- B. 字符之间的间隔时间长度应相同
- C. 通信双方需要同步字符
- D. 字符发送速率由波特率确定

## 7. 同步通信特点

**题目：** 同步通信传输信息时，其特点是（ ）。

**选项：**

- A. 字符之间的传输时间长度可不同
- B. 通信双方必须同步
- C. 每个字符的发送不是独立的
- D. 字符发送速率由数据波特率确定

## 8. 异步串行通信校验方式

**题目：** 在异步串行通信中，常采用的校验方法是（ ）。

**选项：**

- A. 循环冗余码校验
- B. 海明码校验
- C. 奇偶校验
- D. 双重奇偶校验

## 9. 串行传送方式

**题目：** 数据在传送线上一位一位地依次传送，称为 ① 传送方式。

## 10. 串行通信的传送方向

**题目：** 在串行通信数据传送中，通常传送方式有单工、半双工和 ① 等三种。

## 11. 波特率定义

**题目：** 在串行通信中，使用波特率来表示数据的传输速率，它是指 ①。

## 12. 串行通信的两种基本方式

**题目：** 在串行通信中有两种基本的通信方式，即 ① 和同步通信。

## 13. 串并转换实现部件

**题目：** 串行接口中，并行数据和串行数据的转换是利用 ① 实现的。

## 14. 异步通信中“异步”的含义

**题目：** 异步通信中，异步的含义是什么？

## 15. 波特率计算

**题目：** 设数据传送的速率是 `120` 字符/s，而每一个字符格式中，数据位 7 位，停止位、校验位各 1 位，则传送的波特率为多少？
